// console.log or expose in some way an event bubbling from one of the
// links and stop it from bubbling and show this is working.
// you'll need multiple event listeners for this).
//
// As well, stop the links from trying to link to a page that isn't there
// and instead alert the location they're supposed to go to

/* -- Example Event Listener --
$("body")
  .on( "click", "a", function ( event ) {
    console.log( event );
  })
*/
